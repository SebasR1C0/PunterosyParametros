#include <iostream>
using namespace std;
void generar(int datos[]) {
	srand(time(NULL));
	double promedio;
	for (int i = 0; i < 30; i++) {
		datos[i] = (rand() % 4) + 1;
		cout << "La nota de metologia de la maestra " << i + 1 << " es: " << datos[i] << endl;
	}
}
void mayor(int datos[30]) {
	int  m = -1;
	int* maximo = new int[5]{};
	for (int i = 0; i < 30; i++) {
		maximo[datos[i]]++;
	}
	cout << "Hay " << maximo[1] << " maestras con nota 1 " << endl;
	cout << "Hay " << maximo[2] << " maestras con nota 2 " << endl;
	cout << "Hay " << maximo[3] << " maestras con nota 3 " << endl;
	cout << "Hay " << maximo[4] << " maestras con nota 4 " << endl;
	cout << endl;
	for (int i = 1; i < 5; i++) {
		if (m < maximo[i]) {
			m = maximo[i];
		}
	}
	for (int i = 0; i < 4; i++) {
		if (m == maximo[i]) {
			cout << "La mayor votacion fue de " << i << " puntos con "<< maximo[i] << endl;
		}
	}
	delete[] maximo;
}
int main() {
	int* datos = new int[30];
	generar(datos);
	cout << endl;
	mayor(datos);
	delete[]datos;
	return 0;
}
